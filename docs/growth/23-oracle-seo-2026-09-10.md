# Oracle SEO pass — 2026-09-10

## Shipped on main

- Homepage DecisionCluster linking the age + portfolio money pages
- Footer Decisions column
- Meta description aimed at retire-at-50 / $1M queries
- Organization JSON-LD helper
- `src/content/decision-boost-2026-09.ts` — unique pages for `/retire-at-62` and `/can-i-retire-with-400k`

## P0 — restore `src/content/decision-pages.ts` before the next green deploy

Two bad commits replaced the file with placeholder text:

- `f06a0829` Wire September decision pages
- `b937d59d` Restore decision-pages.ts and register septBoostPages

Good copy lives at commit `dd580ca765bd8511624d1676dcf3cde0899d02dc`.

Restore command:

```bash
git checkout dd580ca765bd8511624d1676dcf3cde0899d02dc -- src/content/decision-pages.ts
```

Then add two lines:

```ts
import { septBoostPages } from "@/content/decision-boost-2026-09";
```

and inside `DECISION_PAGES`:

```ts
  retireAtPage(60),
  ...septBoostPages,
  portfolioPage(500_000),
```

Until that file is restored, Vercel should fail the build and keep the current live site. Do not force a deploy.

## P0 — owner index requests (unchanged)

Google Search Console → request indexing on homepage, checkup, money cluster, `/retire-at-62`, `/can-i-retire-with-400k`, Arya series.

Sitemap: https://retirefire.net/sitemap.xml
