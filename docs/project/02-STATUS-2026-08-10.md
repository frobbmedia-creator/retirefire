# Status — 2026-08-10

## Complete

- Published `/pro` Free vs Pro offer: $9 monthly, $79 annual, and $19 one-time report.
- Added server-created Stripe Checkout Sessions.
- Added signature-verified webhook handling for completed checkout and subscription updates/deletions.
- Added reusable checkout buttons and linked Pro teasers to `/pro`.
- Fixed `CheckoutButton` props (`children` + `variant`) to match page usage.
- Added `/pro` to the sitemap.
- Documented Stripe environment and dashboard setup (`docs/project/01-STRIPE-SETUP.md`).

## Remaining (manual — requires Stripe + Vercel dashboard access)

These cannot be completed from the agent (no Stripe account connection; no Vercel env-write tool):

1. **Stripe Dashboard (Test mode first)**
   - Create product “RetireFire Pro”.
   - Create Prices:
     - Recurring monthly $9 → copy `price_...` → `STRIPE_PRICE_ID_MONTHLY`
     - Recurring annual $79 → copy `price_...` → `STRIPE_PRICE_ID_ANNUAL`
     - One-time $19 → copy `price_...` → `STRIPE_PRICE_ID_REPORT`

2. **Vercel → retirefire → Settings → Environment Variables**
   Add for Production + Preview:
   - `STRIPE_SECRET_KEY`
   - `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
   - `STRIPE_WEBHOOK_SECRET`
   - `STRIPE_PRICE_ID_MONTHLY`
   - `STRIPE_PRICE_ID_ANNUAL`
   - `STRIPE_PRICE_ID_REPORT`

3. **Stripe Webhook**
   - Endpoint URL: `https://retirefire.net/api/stripe/webhook`
   - Events: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`
   - Copy signing secret → `STRIPE_WEBHOOK_SECRET`

4. Redeploy after env vars are saved.

5. Test in Stripe test mode, then switch to live keys when ready.

## Later (post-payments)

- Authentication + durable entitlement store
- Webhook event-id deduplication
- Full subscription lifecycle handling

Product invariant: **core calculators stay free forever**.
